{
  "name": "quantum-cubes",
  "private": true,
  "version": "0.1.0",
  "description": "Quantum Cubes — an original mobile-first tile-combination strategy game with a protected Quantum Sandbox and atomic commits.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.0",
    "vitest": "^2.0.5"
  }
}
